# Tasks — 037 workspace-init

- [x] T001 — Generic `board.mjs` template: port the field-proven parser (5 header formats, 2 task
  conventions, warning rules incl. real-WIP and Merged bucket), replace the hardcoded project
  list with workspace.json + auto-detection, add `SDD_WS_ROOT`. Covers: AC-01, AC-02.
- [x] T002 — Verify the template against `lead-platform-workspace`: identical census (105 specs),
  identical rows, real WIP 0. Restore that workspace's own BOARD.md afterwards (the test
  overwrote a generated file — acceptable, regenerated byte-identical). Covers: AC-03.
- [x] T003 — Generic `link-workspace.mjs` template (delimited block, idempotent, `--check`) and
  declarative `drift.mjs` skeleton (CONTRACTS list + worked example; empty ⇒ exit 0 with
  message). Both pass `node --check`. Covers: AC-01, AC-02.
- [x] T004 — Workspace-skill templates (`sdd-status`, `sdd-workspace-link`), `HOW-TO-WORK.md`
  guide with `[fill in]` section, `settings-hooks.json` (3 SessionStart hooks + permissions).
  Covers: AC-01.
- [x] T005 — Orchestrator SKILL.md: 7 phases, consent gates (project list, Graphify runs), stop
  conditions, forbidden list, output format. Delegates mapping to onboarding. Covers: AC-06.
- [x] T006 — Register in `profiles.json` (core profile, next to onboarding) and add the
  cross-reference note to onboarding's Purpose. Covers: AC-04, AC-05.
- [x] T007 — Sync the new/changed skills into `~/.claude-config/skills/` by direct copy — a full
  `install.sh` run would re-add the 7 deliberately-pruned Java reviewers. Covers: —.
