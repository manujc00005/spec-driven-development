# Examples

This folder is a placeholder.

A real, worked example — a small feature carried end-to-end through `/spec-create` → `/spec-plan` → `/spec-analyze` → `/spec-implement` → `/spec-review` → the relevant specialized reviews → `/spec-close` → `/pr-description` — will be added here later, with the actual `SPEC.md`, `PLAN.md`, `TASKS.md`, `DECISIONS.md`, and resulting PR description produced by the workflow.

No example is included yet. Nothing here should be treated as a real feature.
